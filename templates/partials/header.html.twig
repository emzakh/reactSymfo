<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('homepage')}}"><img src="/images/hld.png" class="img-fluid hld" alt=""></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">

            <li class="nav-item">
                <a class="nav-link" href="{{ path('produits_index') }}">Produits</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('recettes_index') }}">Recettes</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('hildegarde') }}">Présentation d'Hildegarde</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('produit_search') }}">Rechercher un produit</a>
            </li>



        </ul>

        <ul class='navbar-nav ml-auto'>
            {% if app.user %}
                <li class='nav-item dropdown'>
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        {{app.user.fullName}}
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        {%  if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('admin_dashboard_index')}}" class="dropdown-item">Administration</a>
                        {% endif %}
                        <a href="{{ path('recette_create')}}" class="dropdown-item">Créer une recette</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('account_index') }}" class="dropdown-item">Mon compte</a>
                        <a href="{{ path('account_profile') }}" class="dropdown-item">Modifer mon profil</a>
                        <div class="dropdown-divider"></div>

                        <a href="{{ path('account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% else %}
                <li class='nav-item'>
                    <a href="{{ path('account_register') }}" class="nav-link">Inscription</a>
                </li>
                <li class='nav-item'>
                    <a href="{{ path('account_login') }}" class="nav-link">Connexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
{# dump(app.flashes) #}
        {% for label, messages in app.flashes %}
            <div class="container">
                <div class="alert alert-{{label}}">
                    {% for message in messages %}
                        <p>{{ message | raw }}</p>
                    {% endfor %}
                </div>
            </div>

        {% endfor %}